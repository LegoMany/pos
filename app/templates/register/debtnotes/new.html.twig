{% extends 'layout.html.twig' %}

{% block body %}
    <div class="row">
        <div class="column">
            <h2>Neue Schuldzettel</h2>
        </div>
    </div>
    {{ form_start(form, {'attr': {'class': 'ui form'}}) }}
    <div class="field">
        <label>Kunde</label>
        {{ form_widget(form.client) }}
    </div>
    <button type="submit" class="ui button sixteen wide field">Speichern</button>
    {{ form_end(form) }}

    {% include 'includes/print/backButton.html.twig' with {'url': path('register_debtnote_list')} %}
{% endblock %}
