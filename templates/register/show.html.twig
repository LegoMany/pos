{% extends 'layout.html.twig' %}

{% block body %}
    <div class="ui grid">
        <div class="row">
            <div class="column">
                <h2>Kasse</h2>
            </div>
        </div>
        <div class="two column row">
            <div class="column">
                <a class="ui button fluid massive" href="{{ path('register_sale_new') }}">Verkauf</a>
            </div>
            <div class="column">
                <a class="ui button fluid massive" href="{{ path('register_debtnote_list') }}">Schuldzetteln</a>
            </div>
        </div>
    </div>

    {% include 'includes/print/backButton.html.twig' with {'url': path('dashboard')} %}
{% endblock %}
