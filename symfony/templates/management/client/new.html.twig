{% extends 'layout.html.twig' %}

{% block header %}
    <a href="{{ path('home') }}" class="item">
        <i class="home icon big"></i>
        POS
    </a>
    <a href="{{ path('management') }}" class="item">
        Verwaltung
    </a>
    <a href="{{ path('management_client_list') }}" class="item">
        Kunden
    </a>
    <a href="#" class="item active">
        Neue Kunde
    </a>
{% endblock %}

{% block actions %}
    <div class="item">
        <button class="ui button green fluid massive" form="form">
            Speichern
        </button>
    </div>
{% endblock %}

{% block body %}
    <div class="ui container">
        {{ form_start(form, {'attr': {'id':'form', 'class': 'ui form'}}) }}
        <div class="field">
            <label>Name</label>
            {{ form_widget(form.name, {'attr': {'autofocus': ''}}) }}
        </div>
        {{ form_end(form) }}
    </div>
{% endblock %}
